# Phase 4 Toolbar Schema

These lightweight JSON files document the state and event shapes used by the floating parameter toolbar prototype. They are documentation/configuration only and require no external validation package.
