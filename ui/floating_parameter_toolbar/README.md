# Floating Parameter Toolbar Prototype

Open `index.html` directly or serve the generated demo folder with:

```bash
python tools/floating_toolbar_phase4.py --output /tmp/cad_bim_toolbar_demo --serve --port 8765
```

The UI is plain HTML/CSS/JavaScript. It has no npm, bundler, CDN, or framework dependency.
